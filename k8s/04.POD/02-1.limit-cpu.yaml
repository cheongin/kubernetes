apiVersion: v1
kind: Pod
metadata:
  name: limit-cpu-1
spec:
  containers:
  - name: limit-cpu-1
    image: vish/stress
    resources:
      limits:
        cpu: "1"  # 최대 CPU 수량(1 Core)
      requests:
        cpu: "0.5"  # 최대 CPU 수량(0.5 Core)
