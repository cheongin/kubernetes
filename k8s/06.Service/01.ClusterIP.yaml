apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-svc
  labels:
    app: nginx-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-svc
  template:
    metadata:
      labels:
        app: nginx-svc
    spec:
      containers:
      - name: nginx-svc
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-clusterip
spec:
  type: ClusterIP
  selector:
    app: nginx-clusterip
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80