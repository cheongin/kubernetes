apiVersion: v1
kind: Pod
metadata:
  name: limit-cpu-1
spec:
  containers:
  - name: limit-cpu-1
    image: vish/stress
    resources:
      limits:
        cpu: "1"
      requests:
        cpu: "0.5"
    args:
    - -cpus
    - "2"
