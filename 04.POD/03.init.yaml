apiVersion: v1
kind: Pod
metadata:
  name: resources-pod
  labels:
    app: resources-pod
spec:
  containers:
  - name: resources-pod
    image: nginx:latest
    resources:
      requests:
        cpu: 0.1
        memory: 200M
      limit:
        cpu: 0.5
        memory: 1G
    ports:
    - containerPort: 8080
